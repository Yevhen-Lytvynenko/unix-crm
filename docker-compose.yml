services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: unixcrm
    ports:
      - "5432:5432"
    volumes:
      - unixcrm_pgdata:/var/lib/postgresql/data

volumes:
  unixcrm_pgdata:
